<version xsi:noNamespaceSchemaLocation="classpath:com/runwaysdk/resources/xsd/version.xsd" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <doIt>
    <create>
      <mdException name="gov.geoplatform.uasdm.bus.InvalidUasComponentNameException" 
                   label="Invalid Name" 
                   description="Thrown when a user enters an invalid name for a Project, Mission, etc." 
                   message="Invalid name. No spaces or special characters such as &lt;, &gt;, -, +, =, !, @, #, $, %, ^, &amp;, *, ?,/, \ or apostrophes are aloud.">
      </mdException>
    
    
      <mdException name="gov.geoplatform.uasdm.bus.DuplicateComponentException" 
                   label="Empty Layer Information" 
                   description="Thrown when someone tries to create a child item whose name conflicts with an existing child name." 
                   message="[{layerName}] already has a [{childComponentLabel}] with the name [{childName}].">
        <attributes>
          <text name="parentName" label="Parent Name" />
          <text name="childComponentLabel" label="Child Component Label" />
          <text name="childName" label="Child Name" />
        </attributes>
      </mdException>
    
    <!-- 
      <mdInformation name="net.geoprism.dashboard.layer.EmptyLayerInformation" label="Empty Layer Information" description="Created when a user creates a layer with no data to display." message="The layer [{layerName}] has no data to display. Either your filter parameters are too restrictive or there is no data for the requested datatype.">
        <attributes>
          <text name="layerName" label="Layer Name" />
        </attributes>
      </mdInformation>
    -->
    
    </create>
    <update></update>
  </doIt>
  <undoIt>
    <delete></delete>
  </undoIt>
</version>